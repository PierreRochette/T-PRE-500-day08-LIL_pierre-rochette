import numpy as np

x_values = np.linspace(x_min, x_max, 100)

# This code will return an array containing 100 elements including x_min and x_max, and 98 other elements with a value between those two numbers. 